class Solution {
  bool canAliceWin(List<int> nums) {
    int singleSum = 0;
    int doubleSum = 0;
    int totalSum = 0;

    for (int n in nums) {
      totalSum += n;
      if (n >= 1 && n <= 9) {
        singleSum += n;
      } else if (n >= 10 && n <= 99) {
        doubleSum += n;
      }
    }

    int bobIfSingle = totalSum - singleSum;
    int bobIfDouble = totalSum - doubleSum;

    return singleSum > bobIfSingle || doubleSum > bobIfDouble;
  }
}

void main() {
  Solution sn = Solution();
  print(sn.canAliceWin([1, 2, 3, 4, 5, 14]));
}
